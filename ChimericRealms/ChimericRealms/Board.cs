using System;
using System.Collections.Generic;
using System.Linq;
using System.Text;
using Microsoft.Xna.Framework;
using Microsoft.Xna.Framework.Graphics;
using Microsoft.Xna.Framework.Content;

namespace ChimericRealms
{
    class Board
    {
        //List<Texture2D> tileHolder = new List<Texture2D>();
        //List<BoardTile> tileList = new List<BoardTile>();

        //int tileSize = 1024; // Size of the board tiles
        //static int boardSize = 3;
        //// int boardLevels = 1;
        //int[] boardArray = new int[8];
        





        //public void loadBoard()
        //{
        //    int h = 0;

        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Alhambra"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Bedlam"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Chapel"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\City"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Fields"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Forest"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Hills"));
        //    tileHolder.Add(Game.Content.Load<Texture2D>(@"Images\Island"));

        //    boardArray[0] = 0;
        //    boardArray[1] = 2;
        //    boardArray[2] = 5;
        //    boardArray[3] = 7;
        //    boardArray[4] = 1;
        //    boardArray[5] = 3;
        //    boardArray[6] = 4;
        //    boardArray[7] = 6;

        //    for (int i = 0; i < boardSize; i++)
        //    {
        //        for (int j = 0; j < boardSize; j++)
        //        {
        //            if (i == 0 || j == 0 || i == 2 || j == 2)
        //            {
        //                tileList.Add(new BoardTile(tileHolder[boardArray[h]], new Vector2(tileSize * i, tileSize * j), new Point(tileSize, tileSize)));
        //                h = h + 1;
        //            }
        //        }
        //    }
        
        
        //}








    }
}
